// Azure Resource Graph Query
// Provides a list of Host Pools that have public access enabled.
resources
| where type == "microsoft.desktopvirtualization/hostpools"
| where properties['publicNetworkAccess'] == "Enabled"
| project recommendationId = "avd-4", name, id, tags, param1="Public Network Access: Yes"
