Resources
| where type == "microsoft.devices/iothubs"
| extend fallbackEnabled=properties.routing.fallbackRoute.isEnabled
| project recommendationId="ioth-6", id, name, resourceGroup, fallbackEnabled
| where fallbackEnabled == false
