// Azure Resource Graph Query
resources
|  where type == "microsoft.containerservice/managedclusters"
|  extend networkProfile = tostring (parse_json(properties.networkProfile.networkPolicy))
|  project recommendationId="aks-8", name, id, param1=strcat("networkProfile:", networkProfile)
