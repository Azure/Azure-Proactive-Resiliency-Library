// Azure Resource Graph Query
// You can use the following Azure Resource Graph query to check if the Application Gateway is either V1 or
| where type =~ 'microsoft.network/applicationgateways'
| extend sku = tostring(properties.sku.name)
| project name, sku
| project  recommendationId = "appgw-4", name, id, resourceGroup
